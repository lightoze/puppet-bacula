Director {
  Name = "<%= @name %>-dir"
  Address = <%= @name %>
  DirPort = <%= @port %>
  Password = "<%= @password %>"

  TLS Enable = yes
  TLS Require = yes
  TLS CA Certificate File = "<%= @tls_ca %>"
  TLS Certificate = "<%= @tls_cert %>"
  TLS Key = "<%= @tls_key %>"
}
