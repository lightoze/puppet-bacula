Director {
  Name = "<%= @name %>-dir"
  Password = "<%= @password %>"

  TLS Enable = yes
  TLS Require = yes
  TLS Verify Peer = yes
  TLS Allowed CN = "<%= @name %>"
  TLS CA Certificate File = "<%= @tls_ca %>"
  TLS Certificate = "<%= @tls_cert %>"
  TLS Key = "<%= @tls_key %>"
}
